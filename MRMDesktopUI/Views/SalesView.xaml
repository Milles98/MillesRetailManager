<UserControl x:Class="MRMDesktopUI.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MRMDesktopUI.Views"
             mc:Ignorable="d" Background="White" FontSize="24"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- Column 0 -->
        <TextBlock Text="Items" Grid.Row="0" Grid.Column="0"/>
        <ListBox x:Name="Products" Grid.Row="1" Grid.Column="0"
                 MinHeight="200" MinWidth="150"/>

        <!-- Column 1 -->
        <StackPanel Orientation="Vertical" Grid.Column="1"
                    Grid.Row="1">
            <TextBlock Text="Quantity"/>
            <TextBox x:Name="ItemQuantity" MinWidth="100"/>
            <Button x:Name="AddToCart" Content="Add to Cart"/>
            <Button x:Name="RemoveFromCart" Content="Remove from Cart"/>
        </StackPanel>

        <!-- Column 2 -->
        <TextBlock Text="Cart" Grid.Row="0" Grid.Column="2"/>
        <ListBox x:Name="Cart" Grid.Row="1" Grid.Column="2"
                MinHeight="200" MinWidth="150"/>
    </Grid>
</UserControl>

<!-- Rows är horizontal, Column är vertical! -->